<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />


    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/salbox_logo_background_red"
        android:label="@string/app_name"
        android:roundIcon="@drawable/salbox_logo_background_red"
        android:supportsRtl="true"
        android:theme="@style/Theme.SalboxDriverApp.MySplash"
        tools:targetApi="31" >
        <service
            android:name=".data.services.LocationService"
            android:foregroundServiceType="location"
            android:exported="false">
            <intent-filter>
                <action android:name="com.salbox.driverapp.action.START_LOCATION_SERVICE"/>
            </intent-filter>
        </service>


        <activity
            android:name=".ui.view.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SalboxDriverApp.MySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SalboxDriverApp"
            />

        <activity
            android:name=".ui.view.ErrorActivity"
            android:exported="true"
            android:theme="@style/Theme.SalboxDriverApp"
            />
    </application>
</manifest>